@startuml
title 图6-x 系统启动初始化流程图

skinparam backgroundColor white
skinparam shadowing false
skinparam defaultFontName "Microsoft YaHei"
skinparam activity {
    BackgroundColor white
    BorderColor black
    FontColor black
}
skinparam ArrowColor black

start

:调用 create_app() 应用工厂函数;
:创建 FastAPI 应用实例;

partition "基础组件注册" {
    :注册 API 路由;
    :注册 JWT 认证与 RBAC 权限依赖;
    :注册请求上下文管理;
    :注册全局异常处理器;
    :注册审计日志与后台任务中间件;
}

:进入生命周期上下文管理器 lifespan;

partition "启动阶段初始化" {
    :执行数据库迁移;
    :加载 YOLOv11 模型权重;
    :创建系统超级用户;
    :初始化系统菜单;
    :同步 API 权限信息;
    :完成角色权限分配;
    :填充食物类别与营养数据;
}

partition "服务就绪检查" {
    :数据库结构检查;
    :模型推理服务预热;
    :核心配置加载检查;
}

:系统启动完成;
:对外提供 Web API 服务;

stop

@enduml