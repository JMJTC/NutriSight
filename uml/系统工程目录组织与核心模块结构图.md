@startuml

left to right direction

skinparam backgroundColor white
skinparam shadowing false
skinparam defaultFontName "Microsoft YaHei"
skinparam packageStyle rectangle
skinparam componentStyle rectangle
skinparam linetype ortho
skinparam nodesep 60
skinparam ranksep 50
skinparam wrapWidth 180

actor "用户" as User
node "浏览器\nBrowser" as Browser

package "项目根目录" as Root {

    package "web/\nVue3 前端工程" as Web {
        component "src/api/\n后端API调用封装" as FE_API
        component "src/router/\n动态路由与导航守卫" as FE_Router
        component "src/store/\nPinia状态管理" as FE_Store
        component "src/views/\n功能页面组件" as FE_Views
        component "src/components/\n通用组件与业务组件" as FE_Components

        component "CrudTable\nCrudModal\nQueryBar" as FE_Common
        component "NutritionDashboard\nNutritionRadarChart\nConfidenceHeatmap\n四步识别向导组件" as FE_Business
    }

    package "app/\nFastAPI 后端应用" as App {

        package "api/v1/\n接口路由层" as API {
            component "RESTful API端点" as API_Route
            component "请求参数校验" as API_Validate
        }

        package "controllers/\n业务控制层" as Controllers {
            component "统一业务入口\n门面模式" as Facade
            component "流程调度与结果封装" as BizFlow
        }

        package "services/\n服务层" as Services {
            component "YoloService\n模型推理服务\n单例模式" as YoloService
            component "FoodRecognitionService\n食物识别管线" as RecognitionService
            component "NutritionRecommendService\n营养推荐引擎" as RecommendService
            component "FoodCatalogService\n食物分类管理" as CatalogService
        }

        package "models/\n数据模型层" as Models {
            component "admin.py\n用户/角色/菜单" as AdminModel
            component "food.py\n食物/营养/识别记录" as FoodModel
        }

        package "schemas/\n数据校验层" as Schemas {
            component "Pydantic请求模型" as ReqSchema
            component "Pydantic响应模型" as RespSchema
        }

        package "core/\n核心模块" as Core {
            component "create_app()\n应用工厂" as AppFactory
            component "JWT认证" as JWT
            component "RBAC权限控制" as RBAC
            component "请求上下文管理" as Context
            component "全局异常处理" as ExceptionHandler
            component "审计日志\n后台任务中间件" as Middleware
            component "通用CRUD基类" as CRUD
        }
    }

    folder "weights/\n模型权重目录" as Weights {
        artifact "best.pt\nYOLOv11分类模型权重" as BestPt
    }

    folder "deploy/\n部署配置目录" as Deploy {
        component "Dockerfile" as Dockerfile
        component "Nginx配置" as Nginx
        component "启动脚本" as StartScript
    }

    database "MySQL数据库" as DB
}

User --> Browser : 访问系统
Browser --> Web : 页面交互

Web --> FE_API
FE_Views --> FE_Components
FE_Components --> FE_Common
FE_Components --> FE_Business
FE_Router --> FE_Store
FE_API --> API_Route : HTTP请求

API_Route --> API_Validate
API_Validate --> Facade
Facade --> BizFlow
BizFlow --> YoloService
BizFlow --> RecognitionService
BizFlow --> RecommendService
BizFlow --> CatalogService

YoloService --> BestPt : 加载模型权重
RecognitionService --> FoodModel
RecommendService --> FoodModel
CatalogService --> FoodModel

AdminModel --> DB
FoodModel --> DB
CRUD --> Models

AppFactory --> API
AppFactory --> Core
JWT --> RBAC
RBAC --> API_Route
Context --> Middleware
ExceptionHandler --> API_Route

Deploy ..> Web : 前端部署
Deploy ..> App : 后端部署
Deploy ..> DB : 数据库部署

@enduml