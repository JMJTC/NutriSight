@startuml


left to right direction

skinparam backgroundColor white
skinparam shadowing false
skinparam defaultFontName "Microsoft YaHei"
skinparam packageStyle rectangle
skinparam componentStyle rectangle
skinparam linetype ortho
skinparam nodesep 50
skinparam ranksep 45
skinparam wrapWidth 160

actor "用户" as User
node "浏览器" as Browser

package "项目根目录" {

    package "web/\nVue3前端工程" as Web {
        component "api/\n接口调用" as WebAPI
        component "router/\n路由管理" as Router
        component "store/\n状态管理" as Store
        component "views/\n页面模块" as Views
        component "components/\n通用与业务组件" as Components
    }

    package "app/\nFastAPI后端应用" as App {
        component "api/v1/\n接口路由层" as API
        component "controllers/\n业务控制层" as Controller
        component "services/\n业务服务层\n模型推理/识别/推荐" as Service
        component "models/\n数据模型层" as Model
        component "schemas/\n数据校验层" as Schema
        component "core/\n核心模块\n认证/权限/异常/CRUD" as Core
    }

    folder "weights/\n模型权重" as Weights {
        artifact "best.pt / last.pt" as Pt
    }

    folder "deploy/\n部署配置" as Deploy

    database "MySQL数据库" as DB
}

User --> Browser : 访问系统
Browser --> Web : 页面交互

Web --> WebAPI
Views --> Components
Router --> Store
WebAPI --> API : HTTP请求

API --> Schema : 参数校验
API --> Controller : 请求转发
Controller --> Service : 业务调度
Service --> Pt : 加载YOLOv11权重
Service --> Model : 数据读写
Model --> DB : ORM映射

Core --> API : 认证与权限控制
Core --> Controller : 异常处理与通用CRUD

Deploy ..> Web : 前端部署
Deploy ..> App : 后端部署
Deploy ..> DB : 数据库部署

@enduml